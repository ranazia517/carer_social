# CARER Website

Deployable static site for CARER by Wellnetix Ltd.

Entry point: `index.html`

A multi-page static HTML/CSS/JS site — no build step, no framework. Copy the `website/` directory to any static host.
